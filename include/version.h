#ifndef VERSION_H
#define VERSION_H

static const int MAJOR_VERSION = 2;
static const int MINOR_VERSION = 0;
static const int BUILD_NUMBER  = 0;  // Start with 0 for the first build for each major AND minor version

#endif